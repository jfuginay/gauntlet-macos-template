Start working on the next Gauntlet task with full context.

Steps:
1. Run `task-master next` to get the highest priority task
2. If no tasks exist, run `task-master list` to see all tasks  
3. Run `task-master show <id>` for detailed task information
4. Start the development server: `npm run dev`
5. Set task to in-progress: `task-master set-status --id=<id> --status=in-progress`
6. Provide implementation plan and first steps

This command gets you ready to tackle the next task with maximum velocity.